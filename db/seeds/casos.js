/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> } 
 */
exports.seed = async function (knex) {
    // Casos já foram inseridos no seed de agentes para manter ordem das foreign keys
    // Este arquivo existe apenas para atender aos requisitos de estrutura
};